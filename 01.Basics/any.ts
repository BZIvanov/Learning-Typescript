// any should not avoided if possible, because we can assign any value without getting errors
// it is useful for cases, when something could be anything
let password: any;

password = '12345';
password = 'secret';
