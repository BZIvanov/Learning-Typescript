// this is the biggest integer we can use without getting unexpected results
const safeInt = Number.MAX_SAFE_INTEGER;

let myBigInt: bigint = BigInt(1234);

let bigInt1 = BigInt(123);

let bigInt2 = 123n;
